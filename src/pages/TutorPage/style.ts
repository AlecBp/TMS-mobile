import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 0,
    marginBottom: 50
  },
  iconImage: {
    flexDirection: "row",
    justifyContent: 'center',
    marginBottom: 30
  },
  bodyParagraph: {
    margin: 40,
    fontSize: 20,
    marginBottom: 30
  },
})