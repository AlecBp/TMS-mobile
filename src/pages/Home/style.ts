import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10
  },
  title: {
    fontSize: 30,
  },
  spaceAround: {
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center"
  }
})